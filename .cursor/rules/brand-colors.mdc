---
description: Qalinraac Academy color palette (strict)
alwaysApply: true
---

# Color Palette (strict — use only these)

## Light Theme

| Role | HEX |
|------|-----|
| Background | `#F7F8FA` |
| Sidebar | `#FFFFFF` |
| Cards | `#FFFFFF` |
| Border | `#E5E7EB` |
| Primary Text | `#111827` |
| Secondary | `#6B7280` |
| Muted | `#9CA3AF` |
| Danger | `#EF4444` |

## Dark Theme

| Role | HEX |
|------|-----|
| Background (page) | `#0A0A0A` |
| Sidebar / header | `#121212` |
| Cards | `#121212` |
| Inset / muted wells | `#1A1A1A` |
| Border | `#2A2A2A` |
| Primary Text | `#F8FAFC` |
| Secondary | `#CBD5E1` |
| Muted | `#94A3B8` |
| Danger | `#EF4444` |

## Mapping

- Page canvas → Background (`--canvas`: light `#F7F8FA` / dark `#0A0A0A`)
- Sidebar / header chrome → Sidebar (`--background`: light `#FFFFFF` / dark `#121212`)
- Cards / popovers → Cards (`--card`: light `#FFFFFF` / dark `#121212`)
- Card borders → Border (`--border`)
- Inset wells / chips on cards → Inset (`--muted` / `--accent` / `--secondary` in dark = `#1A1A1A`)
- Headings / CTAs → Primary Text (`--primary` / `--foreground`)
- Body / descriptions → Muted in dark (`--muted-foreground` `#94A3B8`); Secondary `#CBD5E1` via `--secondary-foreground`
- Placeholders / hints → Muted (`--text-muted`)
- Errors / destructive actions → Danger (`--destructive` / `text-destructive` / `bg-danger` = `#EF4444`)

Do **not** introduce lime, navy, purple, green, or other accent hex outside this table. Legacy `brand-*` class names remap to Primary Text / Background / Cards.
